---
title: Templates
nav_order: 4
has_children: true
---

# Templates

Keramos templates are driven by the `${...}` expression language. This section
covers the syntax, the `$if` / `$each` / `$switch` control-flow directives,
layer merge semantics, hooks, the cluster capability object, and the ~200
built-in template functions.
