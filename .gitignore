/keramos
*.exe
*.test
*.out
/dist/

# Local code-indexer cache (kuzu graph + lance/sqlite search index). Machine-
# local, regenerable, and large; must never be committed.
/.indexer/

# internal docs authoring standard — never commit
docs/STYLE.md
